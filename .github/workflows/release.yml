name: Release

on:
  workflow_dispatch:
    inputs:
      release_kind:
        description: Release kind
        required: true
        type: choice
        options:
          - release
          - pre-release
      game_version:
        description: Vintage Story version used for the updater build and optional game translation
        required: true
        default: "1.22.3"
        type: string
      include_game_translation:
        description: Include projects/game/<game_version>/assets/game/lang/zh-cn.json in the language pack
        required: true
        default: false
        type: boolean

permissions:
  contents: write

env:
  VINTAGE_STORY_BUILD_VERSION: ${{ inputs.game_version }}

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            8.0.x
            10.0.x

      - name: Calculate release version
        id: version
        shell: bash
        run: |
          release_year="$(TZ=Asia/Shanghai date +'%y')"
          release_month_padded="$(TZ=Asia/Shanghai date +'%m')"
          release_month="$((10#${release_month_padded}))"
          version_prefix="${release_year}.${release_month}"

          latest_patch="$(
            git tag --list "v${version_prefix}.*" "${version_prefix}.*" |
              sed -nE "s/^v?${release_year}\.${release_month}\.([0-9]+)$/\1/p" |
              sort -n |
              tail -n 1
          )"

          if [ -z "${latest_patch}" ]; then
            next_patch=1
            latest_patch_text="none"
          else
            next_patch="$((latest_patch + 1))"
            latest_patch_text="${latest_patch}"
          fi

          version="${version_prefix}.${next_patch}"
          tag_name="v${version}"

          if git rev-parse -q --verify "refs/tags/${tag_name}" >/dev/null; then
            echo "Calculated tag ${tag_name} already exists." >&2
            exit 1
          fi

          echo "VERSION=${version}" >> "${GITHUB_ENV}"
          echo "TAG_NAME=${tag_name}" >> "${GITHUB_ENV}"
          echo "version=${version}" >> "${GITHUB_OUTPUT}"
          echo "tag_name=${tag_name}" >> "${GITHUB_OUTPUT}"

          {
            echo "### Release version"
            echo
            echo "- Time zone: UTC+8"
            echo "- Version prefix: ${version_prefix}"
            echo "- Latest patch in current month: ${latest_patch_text}"
            echo "- New version: ${version}"
            echo "- Tag: ${tag_name}"
          } >> "${GITHUB_STEP_SUMMARY}"

      - name: Run tests
        run: dotnet test

      - name: Configure game translation
        shell: bash
        env:
          INCLUDE_GAME_TRANSLATION: ${{ inputs.include_game_translation }}
        run: |
          python3 - <<'PY'
          import json
          import os
          from pathlib import Path

          config_path = Path("config/packer/default.json")
          config = json.loads(config_path.read_text(encoding="utf-8"))
          game_version = os.environ["VINTAGE_STORY_BUILD_VERSION"].strip()
          include_game_translation = os.environ["INCLUDE_GAME_TRANSLATION"].strip().lower() == "true"

          if include_game_translation:
              translation_path = Path("projects/game") / game_version / "assets/game/lang/zh-cn.json"
              if not translation_path.is_file():
                  raise SystemExit(f"Game translation file does not exist: {translation_path}")
              config["gameTranslation"] = {
                  "contentRoot": "projects/game",
                  "targetVersion": game_version,
              }
          else:
              config["gameTranslation"] = None

          config_path.write_text(
              json.dumps(config, ensure_ascii=False, indent=2) + "\n",
              encoding="utf-8",
          )
          PY

          {
            echo "### Game translation"
            echo
            echo "- Game version: ${VINTAGE_STORY_BUILD_VERSION}"
            echo "- Included: ${INCLUDE_GAME_TRANSLATION}"
          } >> "${GITHUB_STEP_SUMMARY}"

      - name: Build package
        shell: bash
        run: |
          dotnet run --project src/Packer -- pack --config config/packer/default.json --package-version "${VERSION}"

      - name: Generate release notes
        shell: bash
        env:
          RELEASE_KIND: ${{ inputs.release_kind }}
        run: |
          notes_path="build/release-notes-${VERSION}.md"
          readme_path="build/README-${VERSION}.md"
          dotnet run --project src/Packer -- describe-release --config config/packer/default.json --package-version "${VERSION}" --release-kind "${RELEASE_KIND}" --fetch-api > "${notes_path}"
          dotnet run --project src/Packer -- describe-package --config config/packer/default.json --package-version "${VERSION}" --fetch-api > "${readme_path}"

      - name: Add README to package
        shell: bash
        run: |
          zip_path="build/VintageStory-Chinese-Language-Package-${VERSION}.zip"
          readme_path="build/README-${VERSION}.md"
          export ZIP_PATH="${zip_path}"
          export README_PATH="${readme_path}"
          python3 - <<'PY'
          from pathlib import Path
          import zipfile
          import os

          zip_path = Path(os.environ["ZIP_PATH"])
          readme_path = Path(os.environ["README_PATH"])

          with zipfile.ZipFile(zip_path, "a", compression=zipfile.ZIP_DEFLATED) as archive:
              archive.write(readme_path, "README.md")
          PY

      - name: Download Vintage Story API
        shell: bash
        run: |
          vintage_story_dir="${RUNNER_TEMP}/vintagestory"
          archive_path="${RUNNER_TEMP}/vs-server.tar.gz"
          mkdir -p "${vintage_story_dir}"
          curl -fsSL "https://cdn.vintagestory.at/gamefiles/stable/vs_server_linux-x64_${VINTAGE_STORY_BUILD_VERSION}.tar.gz" -o "${archive_path}"
          tar -xzf "${archive_path}" -C "${vintage_story_dir}"
          test -f "${vintage_story_dir}/VintagestoryAPI.dll"
          echo "VINTAGE_STORY=${vintage_story_dir}" >> "${GITHUB_ENV}"

      - name: Build updater mod
        shell: bash
        run: dotnet build src/Updater/Updater.csproj -c Release

      - name: Stamp updater mod version
        shell: bash
        run: |
          python3 - <<'PY'
          import json
          import os
          from pathlib import Path

          path = Path("src/Updater/bin/Release/mod/modinfo.json")
          data = json.loads(path.read_text(encoding="utf-8"))
          data["version"] = os.environ["VERSION"]
          path.write_text(json.dumps(data, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")
          PY

      - name: Pack updater mod
        shell: bash
        run: |
          updater_zip="${GITHUB_WORKSPACE}/build/vscnlangpackupdater-${VERSION}.zip"
          mkdir -p build
          (cd src/Updater/bin/Release/mod && zip -r "${updater_zip}" .)

      - name: Create release
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_SHA: ${{ github.sha }}
          RELEASE_KIND: ${{ inputs.release_kind }}
        run: |
          gh release create "${TAG_NAME}" \
            --target "${GITHUB_SHA}" \
            "build/VintageStory-Chinese-Language-Package-${VERSION}.zip" \
            "build/vscnlangpackupdater-${VERSION}.zip" \
            --title "VintageStory-Chinese-Language-Package ${VERSION}" \
            --notes-file "build/release-notes-${VERSION}.md" \
            $( [ "${RELEASE_KIND}" = "pre-release" ] && echo --prerelease )
